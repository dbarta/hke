<div class="container px-4 mx-auto my-8">
  <div class="max-w-6xl mx-auto">
    <div class="flex items-center justify-between mb-4">
      <div class="h4"><%= t("shared.scaffold.edit.title", model: t("contact_person")) + ": #{@contact_person.name}" %></div>
      <%= link_to t("cancel"), @contact_person, class: "btn btn-link" %>
    </div>

    <div class="p-8 bg-white rounded shadow">
      <%= render "form", contact_person: @contact_person %>
    </div>
  </div>
</div>
